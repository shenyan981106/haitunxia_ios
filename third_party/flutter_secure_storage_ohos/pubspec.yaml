name: flutter_secure_storage_ohos
description: Flutter Secure Storage provides API to store data in secure storage. The preferences used in ohos.
version: 1.0.0
repository: https://gitee.com/openharmony-sig/fluttertpc_flutter_secure_storage

environment:
  # ★2026-08-21 本地化时放宽:原 gitee 包写死 <3.0.0,官方 Dart 3(如 Codemagic
  # CI 构建机)严格校验会解析失败;鸿蒙定制 Flutter 构建不受影响
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage_platform_interface: ^1.0.1
  plugin_platform_interface: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.0.12

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      ohos:
        package: com.example.flutter_secure_storage_ohos
        pluginClass: FlutterSecureStorageOhosPlugin